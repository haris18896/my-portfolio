import { Container } from "@mui/material";
import React from "react";

function EmailForm() {
  return (
    <Container
      id="contact"
      maxWidth="lg"
      sx={{ my: 4, px: { xs: 1, sm: 2 } }}
    ></Container>
  );
}

export default EmailForm;
